version: '3.8'

networks:
  app_net:
    driver: bridge

services:
  node:
    build:
      dockerfile: ./docker/node/Dockerfile
      context: .
    ports:
      - '3000'
    volumes:
      - .:/app
    command: sh -c "yarn install && yarn start:dev"
    networks:
      - app_net